{
  "name": "sparkdash-node-agent",
  "version": "0.1.0",
  "description": "Per-node agent for the multi-Spark dashboard. Runs on each DGX Spark (and Narthex). Provides local telemetry, live versions, service catalog, memory budgeting, and actions (docker/systemd/LLM switch) over a local HTTP API.",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "start": "node main.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true,
  "dependencies": {},
  "devDependencies": {}
}
